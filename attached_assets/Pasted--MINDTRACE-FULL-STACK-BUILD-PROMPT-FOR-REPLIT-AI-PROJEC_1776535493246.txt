# MINDTRACE — FULL-STACK BUILD PROMPT FOR REPLIT AI

## PROJECT IDENTITY
App Name: MindTrace
Concept: A dark premium cognitive journaling SaaS — users log thoughts, track emotional patterns, predict cognitive outcomes, and gain insights via AI.

---

## TECH STACK (NON-NEGOTIABLE)
- Framework: Next.js 14 (App Router)
- Language: TypeScript (strict mode)
- Styling: Tailwind CSS + CSS custom properties for theming
- UI: Custom components (no component library dependencies)
- Animation: Framer Motion
- Database: PostgreSQL via Prisma ORM
- Auth: NextAuth.js with Google OAuth provider
- File Storage: Cloudinary (evidence/image uploads)
- Voice: Web Speech API (SpeechRecognition + SpeechSynthesis)
- State: Zustand (client) + TanStack Query (server state)
- Forms: React Hook Form + Zod
- Realtime: Pusher (optional, for live sync)

---

## DESIGN SYSTEM

### Color Palette (CSS variables in globals.css)
```css
:root {
  --bg-primary: #0B0F1A;
  --bg-secondary: #0F1525;
  --glass-bg: rgba(255,255,255,0.04);
  --glass-border: rgba(255,255,255,0.08);
  --accent-purple: #7C3AED;
  --accent-cyan: #06B6D4;
  --accent-blue: #3B82F6;
  --gradient-primary: linear-gradient(135deg, #7C3AED, #06B6D4);
  --text-primary: #F1F5F9;
  --text-muted: #64748B;
  --glow-purple: 0 0 20px rgba(124,58,237,0.4);
  --glow-cyan: 0 0 20px rgba(6,182,212,0.4);
}
```

### Component Patterns
- Cards: `backdrop-blur-xl bg-white/4 border border-white/8 rounded-2xl`
- Buttons: Gradient background + box-shadow glow + scale(1.02) on hover
- Inputs: Dark bg + glowing purple focus ring
- Modals: Blurred backdrop + scale-in animation

### Typography
- Headings: `font-bold tracking-tight` — futuristic weight
- Body: `font-normal leading-relaxed` — readable
- Use Google Fonts: Syne (headings) + DM Sans (body)

---

## FOLDER STRUCTURE (CREATE ALL OF THESE)
/app
layout.tsx              ← Root layout with ThemeProvider
page.tsx                ← Landing page
/auth/page.tsx          ← Google sign-in
/dashboard/page.tsx     ← Main dashboard
/thoughts/page.tsx      ← All thoughts list
/thoughts/[id]/page.tsx ← Thought detail + editor
/timeline/page.tsx      ← Chronological + graph view
/analytics/page.tsx     ← Cognitive analytics
/new-thought/page.tsx   ← Create thought + voice input
/settings/page.tsx      ← Profile + integrations
/api/auth/[...nextauth]/route.ts
/api/thoughts/route.ts  ← GET all, POST create
/api/thoughts/[id]/route.ts ← GET one, PUT update, DELETE
/api/analytics/route.ts
/api/timeline/route.ts
/api/memory/route.ts    ← DELETE wipe memory
/components
/ui
Button.tsx
Card.tsx
Input.tsx
Modal.tsx
Badge.tsx
Spinner.tsx
/layout
Navbar.tsx
Sidebar.tsx
AppShell.tsx
/voice
VoiceRecorderButton.tsx
LiveTranscriptDisplay.tsx
VoiceToThoughtProcessor.tsx
/features
/thoughts
ThoughtCard.tsx
ThoughtEditor.tsx
EmotionSelector.tsx
TagInput.tsx
EvidenceUploader.tsx
PredictionPanel.tsx
/analytics
CognitiveGraph.tsx
InsightCard.tsx
EmotionChart.tsx
/timeline
TimelineView.tsx
TimelineEntry.tsx
/hooks
useVoiceInput.ts
useThoughts.ts
useAnalytics.ts
/store
authStore.ts
themeStore.ts
thoughtStore.ts
voiceStore.ts
/lib
/db/prisma.ts
/auth/options.ts
/utils/cn.ts
/utils/formatDate.ts
/prisma
schema.prisma

---

## PRISMA SCHEMA

```prisma
model User {
  id        String    @id @default(cuid())
  email     String    @unique
  name      String?
  image     String?
  thoughts  Thought[]
  createdAt DateTime  @default(now())
}

model Thought {
  id          String     @id @default(cuid())
  title       String
  context     String
  emotion     String
  intensity   Int        @default(5)
  tags        String[]
  prediction  String?
  evidence    Evidence[]
  userId      String
  user        User       @relation(fields: [userId], references: [id])
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
}

model Evidence {
  id        String   @id @default(cuid())
  url       String
  type      String
  thoughtId String
  thought   Thought  @relation(fields: [thoughtId], references: [id])
}
```

---

## PAGE SPECIFICATIONS

### Landing (/)
- Dark hero with animated neural/face graphic (CSS or SVG)
- Headline: "Understand Your Mind. Predict Your Outcomes."
- CTA: "Start Exploring" → /auth
- 3 feature cards (glassmorphism)
- "How It Works" section (3 steps)
- Footer with logo

### Auth (/auth)
- Centered glass card on dark bg
- Logo + tagline
- Single "Continue with Google" button
- NextAuth signIn("google") on click

### Dashboard (/dashboard)
- Greeting: "Good morning, [name]"
- Quick action: "Log New Thought" button → /new-thought
- Recent thoughts (last 5 ThoughtCards)
- Stats row: total thoughts, avg emotion, streak
- Memory bank section with "Clear Memory" button (confirmation modal)

### New Thought (/new-thought)
- Form fields: Title, Context (textarea), Emotion selector, Tags, Intensity slider
- VOICE INPUT BUTTON (critical):
  - Mic icon button
  - On click: request mic permission → start SpeechRecognition
  - Show live transcript in real-time b...