kind = "web"
previewPath = "/"
title = "MindTrace"
version = "1.0.0"
id = "artifacts/mindtrace"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25203

[services.development]
run = "pnpm --filter @workspace/mindtrace run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mindtrace", "run", "build" ]
publicDir = "artifacts/mindtrace/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25203"
BASE_PATH = "/"
